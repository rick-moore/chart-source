<h2><%= link_to artist.name, artist %></h2>
<h2>Total Songs:<%= artist.songs.count %></h2>
<h2>Total Arrangements:<%= artist.arrangements.count %></h2>
<h3>Songs:</h3>
    <% artist.songs.each do |song| %>
        <p><%= link_to song.name, song %></p>
    <% end %>